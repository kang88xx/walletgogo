import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // API routes that call external chain RPCs run on the Node.js runtime.
  serverExternalPackages: [],
};

export default nextConfig;
